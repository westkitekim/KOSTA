/**
 * step3-function.html 에서 사용하는 함수를 정의
 */

function hello() {
	console.log("hello 함수실행");//함수 호출을 해야 출력된다! 호출하지 않으면 실행안됨
	alert("hello 함수실행~ 맛점하세요");
}
function bye(message) {
	alert(message);
}
function testParam(info, num) {
	alert(info + " " + num);
}






















